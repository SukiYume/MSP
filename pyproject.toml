[build-system]
requires = ["hatchling>=1.27,<1.28"]
build-backend = "hatchling.build"

[project]
name = "radiosonify"
dynamic = ["version"]
description = "Unified, duration-aware sonification for numerical profiles and multidimensional arrays"
readme = "README.md"
license = "MIT AND CC-BY-NC-4.0"
license-files = [
    "LICENSE",
    "src/radiosonify/models/hifigan/LICENSE",
    "src/radiosonify/models/musicnet/LICENSE",
]
requires-python = ">=3.9"
authors = [
    {name = "XiaoQing"},
]
keywords = [
    "astronomy",
    "sonification",
    "radio-pulse",
    "dynamic-spectrum",
    "multidimensional-data",
    "image-sonification",
    "audio",
    "FRB",
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Science/Research",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Scientific/Engineering :: Astronomy",
    "Topic :: Multimedia :: Sound/Audio",
]
dependencies = [
    "numpy>=1.23",
    "scipy>=1.9",
    "librosa>=0.10",
    "soundfile>=0.12",
    "huggingface_hub>=0.20",
    "click>=8.1",
]

[project.optional-dependencies]
hifigan = ["torch>=2.0", "scikit-image>=0.20"]
musicnet = ["torch>=2.0", "tqdm>=4.64"]
rave = ["torch>=2.0"]
all = ["radiosonify[hifigan,musicnet,rave]"]
dev = [
    "build>=1.2",
    "pytest>=7.4",
    "pytest-cov>=4.1",
    "mypy>=1.10",
    "ruff>=0.4",
    "twine>=5.1",
    "vulture>=2.11",
]

[project.scripts]
radiosonify = "radiosonify.cli:main"

[project.urls]
Homepage = "https://github.com/SukiYume/MSP"
Repository = "https://github.com/SukiYume/MSP"
Issues = "https://github.com/SukiYume/MSP/issues"
"Models and data" = "https://huggingface.co/TorchLight/radiosonify"

[tool.hatch.build.targets.sdist]
include = [
    "src/radiosonify",
    "tests",
    "examples",
    "assets",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "LICENSE",
    "MODEL_ASSETS.md",
    "README_CN.md",
    "THIRD_PARTY_NOTICES.md",
]

[tool.hatch.version]
path = "src/radiosonify/__init__.py"

[tool.hatch.build.targets.wheel]
packages = ["src/radiosonify"]

[tool.hatch.build.targets.wheel.force-include]
"src/radiosonify/py.typed" = "radiosonify/py.typed"

[tool.pytest.ini_options]
testpaths = ["tests"]
filterwarnings = [
    "ignore:.*torch.jit.*:DeprecationWarning",
]

[tool.coverage.run]
source = ["radiosonify"]
omit = ["src/radiosonify/models/*"]

[tool.coverage.report]
fail_under = 90
show_missing = true

[tool.mypy]
# Current mypy releases type-check 3.10+ syntax; Python 3.9 compatibility is
# enforced by the runtime CI matrix and Ruff's py39 target.
python_version = "3.10"
files = ["src/radiosonify"]
ignore_missing_imports = true

[tool.ruff]
target-version = "py39"
line-length = 100
# Checkpoint-compatible upstream model definitions retain their source layout.
# See src/radiosonify/models/VENDORED.md; wrappers and tests remain linted.
exclude = ["src/radiosonify/models"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "C90"]
ignore = ["E501"]

[tool.ruff.lint.mccabe]
max-complexity = 10

[tool.ruff.format]
line-ending = "lf"

[tool.vulture]
paths = ["src/radiosonify"]
exclude = ["src/radiosonify/models/"]
min_confidence = 90
